server:
  port: 8083

spring:
  cloud:
    gateway:
      server:
        webflux:
          discovery:
            locator:
              enabled: false
              lower-case-service-id: true
          globalcors:
            cors-configurations:
              '[/**]':
                allowedOriginPatterns:
                  - "http://localhost:*"
                  - "http://127.0.0.1:*"
                allowedMethods:
                  - GET
                  - POST
                  - PUT
                  - PATCH
                  - DELETE
                  - OPTIONS
                allowedHeaders:
                  - "*"
                allowCredentials: true
          routes:
            # User Service - Authentication & User Management
            - id: user-service-api
              uri: lb://user-service
              predicates:
                - Path=/api/users/**,/api/auth/**

            # Administration Service - Patients, Contracts, Observability
            - id: administration-service-api
              uri: lb://administration-service
              predicates:
                - Path=/api/patients/**,/api/contracts/**,/api/observability/**

            # Clinical Service - Audit logs
            - id: clinical-service-api
              uri: lb://clinical-service
              predicates:
                - Path=/api/clinical/**

            # Communication Service - Guardian/staff messaging and appointment requests
            - id: communication-service-api
              uri: lb://communication-service
              predicates:
                - Path=/api/communication/**,/api/appointments/**

            # Procedure Service - Explicit API namespace
            - id: procedure-service-api
              uri: lb://procedure-service
              predicates:
                - Path=/api/procedures/**

            # Pharmacy Service - Rewrite frontend path to service endpoints
            - id: pharmacy-service-api
              uri: lb://pharmacy-service
              predicates:
                - Path=/api/pharmacy/**
              filters:
                - RewritePath=/api/pharmacy/?(?<segment>.*), /$segment

            # Operations Service - Rewrite frontend path to service endpoints
            - id: ops-service-api
              uri: lb://ops-service
              predicates:
                - Path=/api/ops/**
              filters:
                - RewritePath=/api/ops/?(?<segment>.*), /$segment

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${KEYCLOAK_ISSUER_URI:http://localhost:8080/realms/nephrospaidi}

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_DEFAULT_ZONE:http://localhost:8761/eureka}

management:
  endpoint:
    gateway:
      access: read-only
  endpoints:
    web:
      exposure:
        include: health,info,gateway
