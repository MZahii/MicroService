spring:
  application:
    name: clinical-service
  config:
    import: optional:configserver:http://localhost:8888
  flyway:
    outOfOrder: true
    validateOnMigrate: false
    repairOnMigrate: true

# Feign Configuration
feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 10000
        loggerLevel: full
      
      user-service:
        connectTimeout: 3000
        readTimeout: 8000
      
      administration-service:
        connectTimeout: 3000
        readTimeout: 8000
      
      pharmacy-service:
        connectTimeout: 3000
        readTimeout: 8000
      
      communication-service:
        connectTimeout: 3000
        readTimeout: 8000

# Resilience4j Circuit Breaker
resilience4j:
  circuitbreaker:
    configs:
      default:
        registerHealthIndicator: true
        slidingWindowSize: 10
        failureRateThreshold: 50
        slowCallRateThreshold: 50
        slowCallDurationThreshold: 2000
        permittedNumberOfCallsInHalfOpenState: 3
        automaticTransitionFromOpenToHalfOpenEnabled: true
        waitDurationInOpenState: 10000
    
    instances:
      user-service:
        baseConfig: default
      administration-service:
        baseConfig: default
      pharmacy-service:
        baseConfig: default
      communication-service:
        baseConfig: default
  
  retry:
    configs:
      default:
        maxAttempts: 3
        waitDuration: 100
        intervalFunction: exponential
    
    instances:
      user-service:
        baseConfig: default
      administration-service:
        baseConfig: default
      pharmacy-service:
        baseConfig: default

# Service URLs for Feign clients
services:
  user:
    url: ${USER_SERVICE_URL:http://localhost:8086}
  admin:
    url: ${ADMIN_SERVICE_URL:http://localhost:8085}
  pharmacy:
    url: ${PHARMACY_SERVICE_URL:http://localhost:8087}
  communication:
    url: ${COMMUNICATION_SERVICE_URL:http://localhost:8089}

# Management/Actuator
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      show-details: always
  health:
    circuitbreakers:
      enabled: true
