server:
  port: 8090

spring:
  application:
    name: user-service

  datasource:
    url: jdbc:postgresql://ep-cool-breeze-agqhhjlc.c-2.eu-central-1.aws.neon.tech/nephros_user_service_db?sslmode=require&channel_binding=require
    username: neondb_owner
    password: npg_Zb1Kda5LPlin
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  flyway:
    enabled: true
    baseline-on-migrate: true

management:
  endpoints:
    web:
      exposure:
        include: health, info
  endpoint:
    health:
      show-details: never

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL:http://localhost:8761/eureka}

keycloak:
  server-url: http://localhost:8080
  realm: nephrospaidi
  admin:
    client-id: admin-cli
    username: admin
    password: admin123
  auth:
    client-id: nephro-backend
    client-secret: nephro-backend-secret

internal:
  api-key: my-super-secret-internal-key

administration-service:
  base-url: ${ADMINISTRATION_SERVICE_BASE_URL:http://localhost:8087}
